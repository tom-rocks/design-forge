# Frontend only - static site
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd client && npm install"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "cd client && npx serve dist -s -l $PORT"
